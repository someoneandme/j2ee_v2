2016年6月16日 10:00:19

基于redis的超时和CAS，可以做一些好用的工具类：

1. 限制用户自然日、自然月、每小时、每周、每分钟、每年、永久的次数。
参见com.pugwoo.redis.limit
使用方式也非常简单，经过多线程测试通过。这个工具非常好用，性能达到1万一秒，完全满足互联网需求。

2. 分布式系统的全局串行控制，利用redis的set if not exist和expire机制来做。
